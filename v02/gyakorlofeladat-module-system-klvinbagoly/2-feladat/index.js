const utils = require('./utils')
const users = require('./MOCK_DATA')

utils.generateUserList = 'blabla'
utils.getUserNames = 'blabla'

console.log(utils.generateUserList(users));
console.log(utils.getUserNames(users));